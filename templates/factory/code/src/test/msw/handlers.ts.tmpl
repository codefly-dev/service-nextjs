/**
 * MSW request handlers for tests.
 *
 * Define mock API responses here. These are used by both
 * hook tests (renderHook + MSW) and integration tests.
 *
 * Example:
 *   import { http, HttpResponse } from "msw";
 *
 *   export const handlers = [
 *     http.get("/api/items", () => {
 *       return HttpResponse.json([
 *         { id: "1", name: "Test Item", createdAt: "2026-01-01T00:00:00Z" },
 *       ]);
 *     }),
 *   ];
 */

import { http, HttpResponse } from "msw";

export const handlers = [
  // Add your default handlers here
  http.get("/api/health", () => {
    return HttpResponse.json({ status: "ok" });
  }),
];
