"use client";

import Link from "next/link";
import { ArrowRight, Shield, Moon, Code2, Rocket } from "lucide-react";
import { buttonVariants } from "@/components/ui/button";
import { cn } from "@/lib/utils";

const features = [
  {
    icon: Shield,
    title: "Authentication Ready",
    description:
      "Pre-wired auth scaffold with login, signup, and session management. Swap in your provider of choice.",
    step: "01",
  },
  {
    icon: Moon,
    title: "Dark Mode",
    description:
      "System-aware dark mode that works out of the box, powered by next-themes and CSS variables.",
    step: "02",
  },
  {
    icon: Code2,
    title: "Type Safe",
    description:
      "End-to-end TypeScript with strict mode. Catch errors at build time, not in production.",
    step: "03",
  },
  {
    icon: Rocket,
    title: "Deploy Anywhere",
    description:
      "Standalone output ready for Docker, serverless, or edge. No vendor lock-in.",
    step: "04",
  },
];

const techStack = [
  "Next.js",
  "TypeScript",
  "Tailwind CSS",
  "shadcn/ui",
];

export function Hero() {
  return (
    <>
      {/* Hero section with gradient background */}
      <main className="relative flex flex-1 flex-col items-center justify-center px-6 overflow-hidden">
        {/* Grid pattern overlay */}
        <div
          className="pointer-events-none absolute inset-0"
          style={{
            backgroundImage:
              "linear-gradient(to right, oklch(0.5 0 0 / 4%) 1px, transparent 1px), linear-gradient(to bottom, oklch(0.5 0 0 / 4%) 1px, transparent 1px)",
            backgroundSize: "40px 40px",
          }}
        />

        {/* Animated gradient orb */}
        <div
          className="pointer-events-none absolute top-1/2 left-1/2 h-[600px] w-[600px] blur-3xl"
          style={{
            animation: "orb-drift 8s ease-in-out infinite, orb-color 10s ease-in-out infinite",
            background:
              "radial-gradient(circle, oklch(0.55 0.15 260) 0%, oklch(0.45 0.2 280) 40%, transparent 70%)",
            transform: "translate(-50%, -50%)",
            opacity: 0.4,
          }}
        />

        {/* Secondary subtle overlay */}
        <div className="pointer-events-none absolute inset-0 bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-primary/5 via-transparent to-transparent" />

        <div className="relative mx-auto flex max-w-3xl flex-col items-center gap-8 text-center">
          <div className="inline-flex items-center rounded-full border border-border/50 bg-muted/50 px-4 py-1.5 text-sm text-muted-foreground backdrop-blur-sm">
            <span className="mr-2 inline-block h-1.5 w-1.5 rounded-full bg-emerald-500 animate-pulse" />
            Built with codefly
          </div>

          <h1 className="text-5xl font-bold tracking-[-0.02em] sm:text-7xl">
            Build something
            <br />
            <span className="bg-gradient-to-r from-indigo-400 via-purple-400 to-pink-400 bg-clip-text text-transparent">
              remarkable.
            </span>
          </h1>

          <p className="max-w-md text-lg text-muted-foreground leading-relaxed">
            A modern application scaffold with authentication, dark mode, and
            everything you need to ship fast.
          </p>

          <div className="flex gap-4">
            <Link
              href="/signup"
              className={cn(
                buttonVariants({ size: "lg" }),
                "gap-2 shadow-lg shadow-primary/20"
              )}
            >
              Get started
              <ArrowRight className="h-4 w-4" />
            </Link>
            <Link
              href="/dashboard"
              className={cn(
                buttonVariants({ variant: "outline", size: "lg" }),
                "backdrop-blur-sm"
              )}
            >
              Dashboard
            </Link>
          </div>
        </div>
      </main>

      {/* Tech stack bar */}
      <section className="border-t border-border/50 py-8 px-6">
        <div className="mx-auto max-w-6xl flex flex-col items-center gap-4">
          <p className="text-xs font-medium uppercase tracking-widest text-muted-foreground/60">
            Powered by
          </p>
          <div className="flex flex-wrap items-center justify-center gap-x-8 gap-y-3">
            {techStack.map((tech, i) => (
              <span
                key={tech}
                className="flex items-center gap-x-8 text-sm font-medium text-muted-foreground/70"
              >
                {tech}
                {i < techStack.length - 1 && (
                  <span className="ml-8 hidden sm:inline text-muted-foreground/30">|</span>
                )}
              </span>
            ))}
          </div>
        </div>
      </section>

      {/* Features section */}
      <section className="border-t bg-muted/30 py-20 px-6">
        <div className="mx-auto max-w-6xl">
          <h2 className="mb-4 text-center text-2xl font-bold tracking-tight sm:text-3xl">
            Everything you need to get started
          </h2>
          <p className="mb-12 text-center text-muted-foreground max-w-lg mx-auto">
            Production-ready patterns so you can focus on what makes your product unique.
          </p>
          <div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-4">
            {features.map((feature) => (
              <div
                key={feature.title}
                className="group relative flex flex-col items-start gap-4 rounded-xl border border-border/50 bg-card p-6 transition-all duration-300 hover:scale-[1.02] hover:border-primary/30 hover:shadow-lg hover:shadow-primary/5"
              >
                <div className="flex items-center gap-3">
                  <div className="flex h-11 w-11 items-center justify-center rounded-lg bg-primary/10 transition-all duration-300 group-hover:bg-primary/20 group-hover:shadow-md group-hover:shadow-primary/10">
                    <feature.icon className="h-5 w-5 text-primary" />
                  </div>
                  <span className="text-xs font-mono text-muted-foreground/40">
                    {feature.step}
                  </span>
                </div>
                <h3 className="font-semibold">{feature.title}</h3>
                <p className="text-sm text-muted-foreground leading-relaxed">
                  {feature.description}
                </p>
              </div>
            ))}
          </div>
        </div>
      </section>
    </>
  );
}
