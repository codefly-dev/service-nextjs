import type { Plugin } from "@/lib/framework/plugin";

// Plugin registry — module agents add plugin imports here.
// Each plugin self-registers by being included in this array.
export const plugins: Plugin[] = [];
